interface:
  display_name: "to-tickets"
  short_description: "Break a plan, spec, or the current conversation into a set of tracer-bullet tickets, each declaring its blocking edges, published to the configured tracker \u2014 edges as text in a local file, or native blocking links on a real tracker."
policy:
  allow_implicit_invocation: false
