interface:
  display_name: "triage"
  short_description: "Move issues and external PRs through a state machine of triage roles \u2014 categorise, verify, grill if needed, and write agent-ready briefs."
policy:
  allow_implicit_invocation: false
