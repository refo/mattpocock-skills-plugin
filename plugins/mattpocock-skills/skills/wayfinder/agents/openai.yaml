interface:
  display_name: "wayfinder"
  short_description: "Plan a huge chunk of work \u2014 more than one agent session can hold \u2014 as a shared map of investigation tickets on your issue tracker, and resolve them one at a time until the way to the destination is clear."
policy:
  allow_implicit_invocation: false
